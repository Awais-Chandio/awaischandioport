import QueueLessRoleDiagram from "./QueueLessRoleDiagram";

// A project record opts in with `architectureDiagram: "<key>"`; the modal shows
// the matching component under the Architecture tab's text.
export const architectureDiagrams = {
  "queueless-roles": QueueLessRoleDiagram,
};

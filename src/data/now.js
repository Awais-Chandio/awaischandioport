/**
 * "Currently exploring" — the small list under the hero (components/now).
 *
 * To add an item: append a string to the array below.
 * To remove one: delete its line. To change one: edit the string.
 * The widget renders whatever is here, and hides itself if the array is empty.
 *
 * Tip: text before the first " — " is shown a little stronger than the rest,
 * so lead with the topic and put the detail after the dash. Keep to a few
 * items; this is a glance, not a section.
 */
export const nowExploring = [
  "AI application engineering — building projects and learning applied patterns with free-tier LLM APIs",
  "Advanced React Native fundamentals — rebuilding core patterns from scratch, without AI-generated code",
  "Backend development — Node.js, Express, and REST APIs",
];
